# Generated by Containerization Assist using AI
# Deployment manifest for spring-petclinic application
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-petclinic
  namespace: app
  labels:
    app: spring-petclinic
    version: "1.0"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: spring-petclinic
  template:
    metadata:
      labels:
        app: spring-petclinic
        version: "1.0"
    spec:
      containers:
      - name: spring-petclinic
        image: spring-petclinic:1.0
        imagePullPolicy: Never
        ports:
        - name: http
          containerPort: 8080
          protocol: TCP
        resources:
          requests:
            cpu: 500m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 1Gi
        env:
        - name: JAVA_OPTS
          value: "-Xmx768m -Xms512m"
        - name: SERVER_PORT
          value: "8080"
