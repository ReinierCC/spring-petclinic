# Azure Deployment Environment Variables
# Copy this file to .env and fill in your values

# Required Variables
# ==================

# Your Azure subscription ID (get with: az account show --query id -o tsv)
AZURE_SUBSCRIPTION_ID=

# Environment name (e.g., dev, staging, prod)
AZURE_ENV_NAME=petclinic-prod

# Azure region for deployment (e.g., eastus, westus2, westeurope)
AZURE_LOCATION=eastus

# PostgreSQL administrator password (minimum 8 characters)
# Must contain characters from three of the following categories:
# - English uppercase letters
# - English lowercase letters  
# - Numbers
# - Non-alphanumeric characters
POSTGRES_ADMIN_PASSWORD=

# Optional Variables
# ==================

# Custom resource group name (defaults to rg-{AZURE_ENV_NAME})
# AZURE_RESOURCE_GROUP=

# Custom container registry name (defaults to cr{unique-string})
# Must be globally unique, alphanumeric only, 5-50 characters
# CONTAINER_REGISTRY_NAME=

# Custom PostgreSQL server name (defaults to psql-{unique-string})
# Must be globally unique, lowercase, alphanumeric and hyphens
# POSTGRES_SERVER_NAME=

# PostgreSQL admin username (defaults to petclinicadmin)
# POSTGRES_ADMIN_LOGIN=petclinicadmin

# PostgreSQL database name (defaults to petclinic)
# POSTGRES_DATABASE_NAME=petclinic

# Azure principal ID for Key Vault access (optional)
# If not set, uses current user from Azure CLI
# AZURE_PRINCIPAL_ID=

# Local Development Variables (not needed for Azure deployment)
# ==============================================================

# Local PostgreSQL connection
# POSTGRES_URL=jdbc:postgresql://localhost:5432/petclinic
# POSTGRES_USER=petclinic
# POSTGRES_PASS=petclinic
