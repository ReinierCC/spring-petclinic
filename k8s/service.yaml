# Generated by Containerization Assist using AI
# Kubernetes Service manifest for spring-petclinic

apiVersion: v1
kind: Service
metadata:
  name: spring-petclinic
  namespace: app
  labels:
    app: spring-petclinic
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: spring-petclinic
